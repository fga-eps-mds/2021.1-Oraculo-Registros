/*
const db = require("../src/Database");

describe("Test database client setup", () => {
	it("Should start database client", async () => {
		const data = await db.initializeDb();
		expect(data).toEqual(0);
	});
});
*/
